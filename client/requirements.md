## Packages
date-fns | Date formatting and manipulation
lucide-react | Icons
framer-motion | Animations for page transitions and interactions
clsx | Utility for constructing className strings
tailwind-merge | Utility for merging Tailwind classes

## Notes
API endpoints are defined in shared/routes.ts
Dashboard relies on polling or manual refresh to see new uploads
Export feature opens a new window/tab
